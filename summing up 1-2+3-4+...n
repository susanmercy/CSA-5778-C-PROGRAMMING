#include <stdio.h> 
#include <stdlib.h> 
#include <math.h> 
int main() 
{   int n; 
    printf("Enter the number:"); 
    scanf("%d",&n); 
    printf("The sum is %1.0f",pow(-1,n+1)*((n+1)/2)); 
    return 0; 
} 
Here are output scree
