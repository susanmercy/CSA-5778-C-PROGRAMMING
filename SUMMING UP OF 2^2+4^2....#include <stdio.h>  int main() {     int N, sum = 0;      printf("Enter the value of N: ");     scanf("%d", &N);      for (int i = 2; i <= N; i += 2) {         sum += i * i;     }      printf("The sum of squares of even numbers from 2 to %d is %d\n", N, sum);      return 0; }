#include <stdio.h>

int main() {
    int N, sum = 0;

    printf("Enter the value of N: ");
    scanf("%d", &N);

    for (int i = 2; i <= N; i += 2) {
        sum += i * i;
    }

    printf("The sum of squares of even numbers from 2 to %d is %d\n", N, sum);

    return 0;
}
